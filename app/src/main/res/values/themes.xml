<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KidredPawpaws" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_teal</item>
        <item name="android:statusBarColor">@color/primary_teal</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

    <style name="Theme.KidredPawpaws" parent="Base.Theme.KidredPawpaws" />

    <!-- Starting theme to hide default system splash on older Android versions -->
    <style name="Theme.KidredPawpaws.Starting" parent="Theme.KidredPawpaws">
        <item name="android:windowBackground">#F7FFFC</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>
</resources>